# Todo PWA v1.0.6 版面規格

本版以最新確認的手機效果圖為基準，主畫面不再有獨立第三排篩選列。主要尺寸集中於 `layout-config.css`。

| 項目 | CSS 變數 / 規格 | v1.0.6 |
|---|---|---:|
| App 最大寬度 | `--app-max-width` | 430px |
| 左右留白 | `--page-padding` | 12px |
| Header 高度 | `--header-height` | 48px |
| 第一排按鈕高度 | `--tab-height` | 30px |
| 第一排按鈕間距 | `--tab-gap` | 4px |
| 輸入框初始最小高度 | `--input-min-height` | 42px |
| 輸入框最大高度 | `--input-max-height` | 126px |
| 分類 / 優先級 / 日期高度 | `--control-height` | 34px |
| ＋ 按鈕 | `--add-size` | 34px |
| 待辦列最小高度 | `--task-min-height` | 62px |
| 待辦列間距 | `--task-gap` | 0px，以分隔線取代 |
| Checkbox | `--checkbox-size` | 19px |
| 標籤高度 | `--chip-height` | 18px |
| 標籤字體 | `--chip-font-size` | 10px |
| 左側選單寬度 | `--drawer-width` | min(79vw, 306px) |
| 三點浮動選單寬度 | `--action-menu-width` | 138px |
| 一般字體 | `--font-md` | 14px |
| 標題字體 | `--font-lg` | 17px |

## 主畫面由上到下
1. Header：左側三條線、中間「待辦事項」、右側搜尋。
2. 第一排：`全部 / 未完成 / 已完成 / 今天 / 篩選`，五格平均分配。
3. 待辦輸入框：42px 起始高度，依文字內容自動長高，最高 126px。
4. 新增功能列：`分類 / 優先級 / 日期 / ＋` 同一排。
5. 待辦清單：取消獨立卡片間距，改用緊湊水平分隔線。
6. 底部資訊：左側清單統計、右側版本號。

## 第一排篩選按鈕
- 「篩選」永久固定在第一排最右側。
- 不再顯示額外類別 / 優先級篩選列。
- 頁籤管理只排序：全部、未完成、已完成、今天。
- 進階篩選啟用後，篩選按鈕使用淡藍底、藍字並顯示紅色提示點。

## 篩選面板
- 手機寬度：最大 350px。
- 位置：畫面上方約 Header 下緣，背景加半透明遮罩。
- 類別：單一下拉選單。
- 優先級：全部 / 高 / 一般 / 低，以四欄實體按鈕呈現；自訂優先級也會自動加入。
- 日期：開始日期與結束日期同一排。
- 開關：只顯示已完成、只顯示近期 7 天。
- 底部：清除 / 取消 / 套用。

## 待辦清單
- 欄位：左 29px 完成框 / 中間自適應內容 / 右 30px 三點按鈕。
- 完成框與三點按鈕均使用垂直置中。
- 淺色模式三點：`var(--text)` → 黑色系。
- 深色模式三點：`var(--text)` → 白色。
- 分類、優先級、日期標籤使用 18px 高度的小型矩形標籤。
- 到期日為今天時顯示「今天」淡紅標籤；逾期且未完成使用警示淡紅標籤。

## 手機響應
- 主要針對 375–430px 寬 Android / iPhone。
- 374px 以下：按鈕字體縮小、功能列與篩選優先級按鈕間距縮小。
- 560px 以下 App 滿版顯示。
- 支援 `safe-area-inset-top` / `safe-area-inset-bottom`，避免 iPhone 瀏海與 Home Indicator 遮擋。

## 左側選單
- 待辦事項
- 日曆
- 分類管理
- 優先級管理
- 備份與還原
- 頁籤管理
- 設定
- 關於

## 資料與版本
- IndexedDB 名稱：`todo-pwa-db`。
- Service Worker 快取：`todo-pwa-cache-v1.0.6`。
- App 版本：`1.0.6`。
- 舊版本資料不因 UI 更新而重建或清除。
